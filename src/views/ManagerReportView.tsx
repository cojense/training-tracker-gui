import { Typography, Box } from '@mui/material';

export const ManagerReportView = () => {
  return (
    <Box>
      <Typography variant="h4">Manager Report</Typography>
      <Typography variant="body1">
        This page will display reports for training managers.
      </Typography>
    </Box>
  );
};
