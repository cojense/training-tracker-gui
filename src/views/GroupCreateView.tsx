import { Typography, Box } from '@mui/material';

export const GroupCreateView = () => {
  return (
    <Box>
      <Typography variant="h4">Create New Group</Typography>
      <Typography variant="body1">
        This page will contain the form for creating a new group.
      </Typography>
    </Box>
  );
};
