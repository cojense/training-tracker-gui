import { Typography, Box } from '@mui/material';

export const SupervisorView = () => {
  return (
    <Box>
      <Typography variant="h4">Supervisor View</Typography>
      <Typography variant="body1">
        This page will display reports for supervisors.
      </Typography>
    </Box>
  );
};
