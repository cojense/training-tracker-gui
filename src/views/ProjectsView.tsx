import { Typography, Box } from '@mui/material';

export const ProjectsView = () => {
  return (
    <Box>
      <Typography variant="h4">Projects List</Typography>
      <Typography variant="body1">
        This page will display a list of all projects.
      </Typography>
    </Box>
  );
};
