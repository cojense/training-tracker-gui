import { Typography, Box } from '@mui/material';

export const GroupsView = () => {
  return (
    <Box>
      <Typography variant="h4">Groups List</Typography>
      <Typography variant="body1">
        This page will display a list of all groups.
      </Typography>
    </Box>
  );
};
