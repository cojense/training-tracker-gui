import { Typography, Box } from '@mui/material';

export const RecordTrainingEventView = () => {
  return (
    <Box>
      <Typography variant="h4">Record Training Event</Typography>
      <Typography variant="body1">
        This page will contain the form for recording a training event.
      </Typography>
    </Box>
  );
};
