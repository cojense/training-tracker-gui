import { Typography, Box } from '@mui/material';

export const UserListView = () => {
  return (
    <Box>
      <Typography variant="h4">User List</Typography>
      <Typography variant="body1">
        This page will display a list of all users in the system.
      </Typography>
    </Box>
  );
};
