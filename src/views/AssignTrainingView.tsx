import { Typography, Box } from '@mui/material';

export const AssignTrainingView = () => {
  return (
    <Box>
      <Typography variant="h4">Assign Training</Typography>
      <Typography variant="body1">
        This page will contain the form for assigning training to groups or
        users.
      </Typography>
    </Box>
  );
};
